export function pullProducts() {
  //This function will be run on a cron job
  // It will call the api to pull all the products from the woocommerce store
}

export function syncProducts() {
  //Run a comparison between the pulled products and the existing products in the database
}

export function deleteProducts() {
  //Implement the logic to delete products from the database
}

export function insertProducts() {
  //Implement the logic to insert products into the database
}
